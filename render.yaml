services:
  - type: web
    name: voxform-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /health
    envVars:
      - key: MIGRATIONS_PATH
        value: /app/migrations
      - key: STORAGE_PATH
        value: /tmp/storage
      # ── Set the values below manually in the Render dashboard ──────────────
      # DATABASE_DSN  = mysql+aiomysql://user:pass@host:port/voxform
      #                 (use Clever Cloud MySQL free tier — see setup notes)
      - key: DATABASE_DSN
        sync: false
      # JWT_SECRET    = any strong random string (openssl rand -hex 32)
      - key: JWT_SECRET
        sync: false
      # STORAGE_URL   = https://voxform-api.onrender.com/storage
      - key: STORAGE_URL
        sync: false
      # APP_URL       = https://your-app.vercel.app  (used in email verification links)
      - key: APP_URL
        sync: false
      # CORS_ORIGINS  = https://your-app.vercel.app
      - key: CORS_ORIGINS
        sync: false
      # ── Optional AI keys (leave blank to skip) ────────────────────────────
      - key: GROQ_API_KEY
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
